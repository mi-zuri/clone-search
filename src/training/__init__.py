"""Training utilities and loops."""

# Import directly from modules to avoid circular import when running as __main__:
#   from src.training.train_encoder import nt_xent_loss, compute_pos_weights, ...
